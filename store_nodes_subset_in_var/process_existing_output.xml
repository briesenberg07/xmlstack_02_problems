<?xml version="1.0" encoding="UTF-8"?>
<output>
   <output_set id="ABC">
      <prop id="001"/>
      <implementations>2</implementations>
      <!--$sorted_props_all_nodes stores *all* implementations w/ matching match values,
		I only want the *single* implementation matching my desired output-->
      <prop id="002"/>
      <implementations>2</implementations>
      <!--$sorted_props_all_nodes stores *all* implementations w/ matching match values,
		I only want the *single* implementation matching my desired output-->
   </output_set>
   <output_set id="CBA">
      <prop id="002"/>
      <implementations>2</implementations>
      <!--$sorted_props_all_nodes stores *all* implementations w/ matching match values,
		I only want the *single* implementation matching my desired output-->
      <prop id="001"/>
      <implementations>2</implementations>
      <!--$sorted_props_all_nodes stores *all* implementations w/ matching match values,
		I only want the *single* implementation matching my desired output-->
   </output_set>
   <output_set id="AAA">
      <prop id="003"/>
      <implementations>1</implementations>
      <!--$sorted_props_all_nodes stores *all* implementations w/ matching match values,
		I only want the *single* implementation matching my desired output-->
   </output_set>
</output>
